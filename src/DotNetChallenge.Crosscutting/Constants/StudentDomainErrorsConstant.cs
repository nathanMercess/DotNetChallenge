namespace DotNetChallenge.Crosscutting.Constants;

public static class StudentDomainErrorsConstant
{
    public static readonly string INVALID_STUDENT_ID = nameof(INVALID_STUDENT_ID);
    
    public static readonly string INVALID_STUDENT_NAME = nameof(INVALID_STUDENT_NAME);
    
    public static readonly string INVALID_STUDENT_USER = nameof(INVALID_STUDENT_USER);

    public static readonly string INVALID_STUDENT_PASSWORD = nameof(INVALID_STUDENT_PASSWORD);
}