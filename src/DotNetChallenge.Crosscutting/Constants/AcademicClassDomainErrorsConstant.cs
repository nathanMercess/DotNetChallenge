namespace DotNetChallenge.Crosscutting.Constants;

public static class AcademicClassDomainErrorsConstant
{
    public static readonly string INVALID_ACADEMIC_CLASS_ID = nameof(INVALID_ACADEMIC_CLASS_ID);

    public static readonly string INVALID_COURSE_ID = nameof(INVALID_COURSE_ID);

    public static readonly string INVALID_CLASS_NAME = nameof(INVALID_CLASS_NAME);

    public static readonly string INVALID_ACADEMIC_CLASS_YEAR = nameof(INVALID_ACADEMIC_CLASS_YEAR);
}
